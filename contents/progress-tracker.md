# Progress Tracker

Update this file whenever the current phase, active feature , or implementation state changes

## Current Phase

Core functionality implementation (Local Documents & Notifications).

## Current Goal

Polished empty states, loading states, and error states for robust app completion.

## Completed

- **Feature (Expanded Mock Data Seeding)**: Updated the "Seed Demo Documents" action to generate 12 custom folders ("Personal", "Work", "Finance", "Health", "Travel", "Vehicle", "Legal", "Property", "Utilities", "Education", "Insurance", "Other") and 76 realistic documents with varied date spans (ranging from expired to near-expiry and long-term expiries) to facilitate layout testing, sorting accuracy, and scroll performance audits under load. Verified 100% type-safety and clean ESLint checks (0 errors).
- **Feature (Documents Screen Design Polish)**: Polished the visual layout, spacing, colors, interactive states, and typography of the Documents tab screen (`src/app/(tabs)/documents.tsx`) and the `DocumentCard` component (`src/components/DocumentCard.tsx`) following the `impeccable` guidelines. Replaced hardcoded `#B3B3B3` colors on non-selected selection circles, chevrons, and options icons with Zentra's secondary color token (`colors.secondary` / `#636363`) for optimal contrast and consistency. Removed the header "Select" button as users can long-press documents/folders to trigger selection mode, simplifying header layout. Resolved visual overlaps of `ExpiryBadge` elements in both grid and list views by removing absolute positioning overlays and integrating the badge inline using a new `hideExpirySafe` prop, allowing the layout to reflow naturally. Resized the grid/list toggle buttons to `w-11 h-11` and `rounded-xl` and updated the active/inactive color routing. Polish-refined the Active Category Header Back button, the Folder options icon buttons, and the Folder Input modal input focus states to completely avoid visual jitter and layout shifts (constant `border-2` with dynamic colors). Standardized Folder modal Cancel/Save buttons to match the typography and layout height (`min-h-[48px]`, `py-3.5`, `font-display`, `font-semibold`) of the standard `ConfirmationModal` component. Removed unused react-native imports and resolved lint warnings. Verified 100% type-safety using `bunx tsc --noEmit` and clean ESLint checks (0 errors).
- **Feature (Expiry Date Sorting on Home & Documents Screens)**: Implemented a sorting feature to display documents by their nearest expiry date. On the Documents tab screen (`src/app/(tabs)/documents.tsx`), updated the default sort mode from `"added"` (date added descending) to `"expiry"` (expiry date ascending/soonest first), ensuring users immediately see the most pressing expirations. On the Home screen (`src/app/(tabs)/index.tsx`), added a sort state defaulting to `"expiry"` and replaced the static "Recent Documents" section header with a dynamic header containing a clean segment control toggle ("Expiry" vs "Recent") and dynamic title ("Upcoming Expirations" vs "Recent Documents"). Sorted home screen documents using the `sortByExpiry` utility when "Expiry" is active, and updated `RecentDocRow` to render the `ExpiryBadge` component inline with document metadata. Verified type safety with `bunx tsc --noEmit` and clean lint formatting with `bun run lint` (0 errors).
- **Feature 35 (Recently Deleted - Soft Delete with 30-Day Recovery)**: Implemented a soft-delete system in Zentra. Deleted documents are moved to a "Recently Deleted" bin (with `isDeleted` and `deletedAt` metadata) and are permanently purged from the database and local disk after 30 days. Updated store selectors to filter out soft-deleted documents across all listings (Home, Documents, Favorites, Alerts, Search, and Dashboard unread counts). Created `src/app/recently-deleted.tsx` trash list screen with info notice bar, count badge, native swipe action gestures (green Restore and red Delete Forever), Web/accessibility direct button overrides, and a full-width destructive "Delete All Permanently" action. Configured a "Recently Deleted" navigation row in the Profile settings screen under the Data section showing active trash counts. Enabled a background cron-like `purgeExpiredTrash()` execution inside the root layout on app startup to clean up documents deleted more than 30 days ago. Verified 100% type-safety using `tsc --noEmit` and clean ESLint checks.
- **Feature 34 (Import / Restore from Backup)**: Implemented an import data screen under `src/app/import-data.tsx`. Users can select a Zentra JSON backup file (via native `expo-document-picker` or standard HTML file input on Web) or paste raw backup JSON text. The system parses, validates the format, and provides an interactive preview of backup documents (detailing new documents vs. existing duplicates that will be skipped, and listing the first 5 entries). Merges imported documents into the Zustand database, automatically setting `notificationsEnabled: false` by default on all imported records to preserve privacy and prevent stale reminders. Added the "Import Data" row in the Profile settings tab screen `src/app/(tabs)/profile.tsx` under the Data section card. Verified 100% type-safety and clean ESLint checks.
- **Feature 33 (Export Data)**: Implemented a local document metadata backup export screen under `src/app/export-data.tsx`. Users can export a formatted JSON file of all their document metadata (names, categories, expiry dates, notes, and notification settings) and share it using the native system share sheet or download it directly on web browsers, ensuring raw file attachments and device-specific paths (`localUri`) are excluded to preserve Zentra's 100% privacy-first rule. Added an "Export Data" row in the Profile tab settings screen `src/app/(tabs)/profile.tsx` under the Data card above "Delete All Documents". Verified 100% type-safety with `tsc --noEmit` and clean compilation with ESLint.
- **Bug Fix (Android Exact Alarm Permission & Custom Notification Channel)**: Declared `android.permission.SCHEDULE_EXACT_ALARM` and registered `"expo-notifications"` plugin in [app.json](file:///d:/MyProjects/Expo_Projects/Zentra/app.json) to allow the Android OS to schedule precise date-based alarms on Android 12+ (API 31+). Migrated the on-device notification channel ID from `"default"` to `"zentra-alerts"` (named `"Zentra Expiry Alerts"`) inside [notifications.ts](file:///d:/MyProjects/Expo_Projects/Zentra/src/lib/notifications.ts) to bypass immutable low-priority settings or system-muted channel states, and updated trigger configurations to route reminders accordingly. Corrected the module-level foreground notification behavior handler to satisfy standard TypeScript interface signatures. Checked type safety with `bunx tsc --noEmit` (0 errors) and validated lint formatting with `bun run lint` (0 errors).
- **Bug Fix (Notification Channel and Scheduling)**: Fixed the issue where local notifications (both default and custom) were not triggering on the user's phone. Ensured the default Android notification channel is registered dynamically in `scheduleDocumentNotifications`, added the required `channelId: "default"` to the trigger parameters in `Notifications.scheduleNotificationAsync`, and modified the `InitialLayout` mount hook in `src/app/_layout.tsx` to automatically call `requestPermissions` on startup. Also bypassed simulator environment restrictions (`Device.isDevice` check) to permit local notification scheduling and verification in testing environments. Checked type-safety with `bunx tsc --noEmit` compiling with 0 errors.
- **Feature (Help & FAQ Screen)**: Implemented a searchable Help & FAQ screen under `src/app/help.tsx` containing static accordion-style question rows with rotated chevron icons, search filtering, and custom `EmptyState` fallbacks. Added a "Contact Support" row pre-addressed to the support email via the device mail client. Linked the page from `src/app/(tabs)/profile.tsx` under the About card above Privacy Policy and Terms of Service. Verified 100% type safety and clean ESLint checks.
- **Bug Fix (Recent Searches Auto-Saving)**: Restored the 300ms input debounce history saving logic in [search.tsx](file:///d:/MyProjects/Expo_Projects/Zentra/src/app/search.tsx) and configured search queries to also automatically save to history whenever a user selects a search result. This resolves the issue where recent searches was not saving or displaying when users navigated directly to search results or exited the search screen without explicitly pressing search/enter on their keyboard. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Feature (App Lock (Biometric + PIN))**: Implemented an optional biometric and PIN-based App Lock screen and layout enforcement system. Created a custom `useAppLock` React hook that reactive-syncs settings state across screens without using Zustand. Refactored the hook and configuration screen (`src/app/app-lock.tsx`) to dynamically check native module availability (`requireOptionalNativeModule("ExpoLocalAuthentication")`) and load `expo-local-authentication` using dynamic asynchronous `import()` at runtime, completely avoiding eager evaluation startup crashes on unlinked environments (Web, Expo Go, and emulators). Developed a custom App Lock settings screen supporting toggling, test authentication with inline alerts, and available method detection, with graceful sandbox overrides when native authentication is missing. Wired the root layout (`src/app/_layout.tsx`) with an `AppState` transition listener that displays a full-screen Zentra-branded `Modal` overlay when transitioning to active foreground from background/inactive. Configured the overlay to automatically prompt for verification and block all interaction until successfully authenticated. Connected the App Lock settings row inside the Profile screen (`src/app/(tabs)/profile.tsx`) to show live lock status and link directly to the settings page. Resolved unescaped entity errors in `privacy-policy.tsx` and `terms-of-service.tsx` to satisfy project ESLint policies. Verified full type safety with `bunx tsc --noEmit` and clean linter checks with `bun run lint`.
- **Feature (Terms of Service Screen)**: Created a dedicated scrollable `Terms of Service` screen under `src/app/terms-of-service.tsx` displaying the Zentra terms of service across 10 numbered sections. Styled to match a premium layout structure: a large left-aligned screen header title, a full-width bottom card container with top-rounded corners (`rounded-t-[32px]`), an uppercase notice header, and a circular bullet list of commitments. Used the centralized `src/constants/app.ts` support email constant `APP_SUPPORT_EMAIL` with an actionable contact email pressable (opening user's mail client via `Linking`). Integrated the Terms of Service navigation row inside `src/app/(tabs)/profile.tsx` under the About settings block, positioned immediately below the Privacy Policy row. Added styled and tappable legal links pointing to both the Terms of Service and Privacy Policy screens inside the Sign-Up screen (`src/app/(auth)/sign-up.tsx`) below the Sign Up button, utilizing `colors.accent` and `text-[12px]`. Verified 100% type-safety using `bunx tsc --noEmit`.
- **Feature (Privacy Policy Screen)**: Created a dedicated scrollable `Privacy Policy` screen under `src/app/privacy-policy.tsx` displaying the Zentra privacy policy across 9 numbered sections. Styled with the redesigned structure: a large left-aligned screen header title, top-rounded full-width bottom card container (`rounded-t-[32px]`), uppercase notice header, and circular bulleted commitment summary list. Added a centralized `src/constants/app.ts` file to export `APP_SUPPORT_EMAIL = "support@zentra.app"` to avoid hardcoded support email values. Integrated a dynamic email link using React Native's `Linking` API to open the user's email client directly. Added the Privacy Policy entry point row inside `src/app/(tabs)/profile.tsx` under the About settings block, positioned immediately below the App Version row, with full active state tapping feedback and accessibility settings. Verified 100% type-safety using `bunx tsc --noEmit`.
- **Feature (Custom Expiry Reminder Days and Times)**: Implemented support for custom notification advance days and time of day reminders inside Zentra's Profile screen. Updated the `NotificationSettings` schema and the global Zustand store to persist the custom configuration locally. Enhanced the on-device notification scheduling library to schedule notification triggers at the custom hours and minutes. Wired dynamic rescheduling of all active notifications when settings change. Built custom interactive UI controls, including a text input modal for adding custom day offsets, a close button for quick deletion of custom days, and a slide-up digital TimePickerModal bottom sheet for native devices (falling back to native time input overlays on Web). Verified type-safety with `bunx tsc --noEmit` returning zero compilation errors.
- **Feature (Multi-Selection and Bulk Deletion of Files and Folders)**: Added multi-selection and bulk deletion features on both the Documents and Home screens, triggered via standard Select buttons or long-press (onHold) interactions. Created atomic Zustand store actions `deleteMultipleDocuments` and `deleteMultipleFolders` for batch state updates. Modified `DocumentCard` component to accept selection flags and render checked/unchecked circle states. Wired card press and onLongPress handlers across `src/app/(tabs)/documents.tsx` and `src/app/(tabs)/index.tsx` to toggle selection and coordinate states. Configured a premium floating bottom action bar displaying selection counts alongside Cancel and Delete controls. Integrated custom design-compliant ConfirmationModals for bulk deletion, which automatically cancel scheduled local notifications and purge persistent file attachments from the device storage directory upon user confirmation. Verified type safety with `bunx tsc --noEmit` compiling with 0 errors.
- **Feature (Rich Sharing Options & HTML Document Export with Inline Images)**: Enhanced the sharing functionality on the Document Details screen (`src/app/document/[id].tsx`) to support sharing both metadata details AND image attachments together. Introduced an interactive share options bottom sheet `<ActionSheet>` (toggled by the "Share" row when the document contains an attachment) offering three granular options: (1) "Share Details as Text" to share metadata details (name, expiry date, category, notes) as plain text; (2) "Share Raw File" to share the attachment file directly (utilizing native `expo-sharing` or falling back to iOS share sheet); and (3) "Share Details + Image (HTML)" (for image attachments) which reads the local image file as a base64 string, dynamically generates a beautifully styled responsive HTML document matching Zentra's design tokens (linear gradients, badges, cards, and image sizing), and shares it as a self-contained `.html` file. Developed the HTML template generator in `src/lib/share.ts` and integrated a clean fallback structure using React Native's core `Share` API for environments without native sharing support. Restored dynamic evaluation prechecks (`requireOptionalNativeModule`) to completely avoid Metro require crashes on unlinked environments, and added graceful fallback prompts on Android to allow sharing the plain text details when file sharing is unavailable. Verified type safety and clean compile using `bunx tsc --noEmit`.
- **Feature (Custom Confirmation Modals for Design-System-Compliant Action Prompts)**: Replaced default OS and browser-level `Alert.alert` confirm prompts for deleting documents, deleting folders, signing out, and wiping documents database with a custom-built `<ConfirmationModal>` component styled with NativeWind. Created a centered popup card layout containing contextual warning/destructive or informational headers, Outfit/Inter typography alignment, dynamic message styling, and button structures. Integrated it across the Home, Documents, Document Details, and Profile screens to guarantee a premium and visually uniform experience on iOS, Android, and Web platforms. Verified full type-safety with `bunx tsc --noEmit` returning zero compiler errors.
- **Bug Fix (ImagePicker Temporary File Garbage Collection)**: Resolved a native `java.io.FileNotFoundException` (`ENOENT`) crash caused by the operating system garbage-collecting temporary image files from the app's `cache/ImagePicker` directory. Created a utility function `saveFilePermanently` in `src/lib/share.ts` to copy picked attachments immediately from temporary paths to the app's permanent `documentDirectory`. Wrapped form submission handlers in both `src/app/add-document.tsx` and `src/app/edit-document/[id].tsx` to save attachments permanently on document creation or edit, and automatically clean up deprecated permanent attachments when they are replaced or removed. Verified type safety with `bunx tsc --noEmit`.
- **Feature (Move, Share, and Download Functionalities)**: Implemented complete and robust Move and Download options on the Document Details screen (`src/app/document/[id].tsx`), replacing the decorative placeholders. Move triggers a new custom `<ActionSheet>` populated with the store's `folders` list and updates categories on-device. Download triggers the browser download manager on Web, Storage Access Framework (SAF) folder picker on Android to store files in any directory (e.g. `Downloads`), and Expo Sharing "Save to Files" on iOS. Fixed native `expo-sharing` crashes and false-negatives by replacing dynamic JSI checks with a cleaner `isAvailableAsync()` call and adding a Web Share API fallback on Web. Further safeguarded both Share and Download by dynamically wrapping `require('expo-sharing')` with a helper `getSharingModule()` to completely prevent fatal `Cannot find native module 'ExpoSharing'` runtime errors on environments/builds missing this native module. Verified type safety with `bunx tsc --noEmit` compiling with 0 errors.
- **Feature (Entrypoint Migration to TypeScript)**: Migrated the root application entry point `index.js` to `index.ts`. Created the `GlobalWithFlags` TypeScript interface to define the optional boolean properties `__isImagePickerNativeAvailable` and `__isDocumentPickerNativeAvailable` on `globalThis`, casting safely to prevent compiler errors in strict mode. Updated `"main"` in `package.json` to point to `index.ts`. Verified type-safety across the entire codebase with `bunx tsc --noEmit` returning 0 errors.
- **Bug Fix (Native Module Availability - ExpoUpdates)**: Fixed a startup crash (`Route "./_layout.tsx" is missing the required default export` / `Cannot find native module 'ExpoUpdates'`) on environments without `ExpoUpdates` native module binary linked (e.g. Expo Go / unlinked simulators). Resolved by removing the static import of `expo-updates` in `src/components/ErrorScreen.tsx` and dynamically checking availability using `requireOptionalNativeModule("ExpoUpdates")` from `expo-modules-core` at runtime before calling `require("expo-updates")`.
- **Feature 29 (Custom ActionSheet / Options Menu)**: Created a premium custom `<ActionSheet>` bottom sheet options menu component (`src/components/ActionSheet.tsx`) styled using Zentra design system tokens (translucent backdrop, top-rounded corners, and Feather icons). Replaced generic OS-level `Alert.alert` lists with this custom `<ActionSheet>` inside the Document Details screen (`src/app/document/[id].tsx`) and Folders management list screen (`src/app/(tabs)/documents.tsx`). Fully wired the previously disabled three-dots options button on the Home screen's Recent Documents list (`src/app/(tabs)/index.tsx`) to trigger the custom `<ActionSheet>` options for the selected document, permitting editing and deletion directly from the dashboard. Verified 100% type safety and successful compilation using `bunx tsc --noEmit`.
- **Feature 28 (Accessibility Audit - VoiceOver + TalkBack)**: Audited all screens and components in the codebase and implemented built-in React Native accessibility properties. Set `accessibilityLabel` on all icon-only buttons (FAB, back navigation, clear buttons, favorite stars, and three-dot menus). Added `accessibilityRole` to all interactive rows, tabs, sort pills, and switches (including mapping `accessibilityState={{ checked: enabled }}` on switch controls). Added `accessibilityLabel` on all form TextInputs (Document name, size labels, notes, verification codes, and emails/passwords in the auth screens). Ensured all touch targets are at least `44x44` points by applying standard `hitSlop` objects (`hitSlop={{ top: 8, bottom: 8, left: 8, right: 8 }}` or `12px` equivalents). Configured screen reader announcements (`AccessibilityInfo.announceForAccessibility`) on document deletion and form save events. Adjusted secondary text color contrast to be WCAG AA compliant (>4.5:1) by darkening the secondary text token to `#636363` (giving 5.6:1 contrast ratio). Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Feature 27 (Error Boundary + Crash Handling)**: Wrapped the entire app in a global `ErrorBoundary` using a custom React class component that logs errors locally (privacy-first, no external crash reporters) and falls back to a clean, user-friendly `ErrorScreen` recovery layout on crash. Styled `ErrorScreen` using design system tokens with custom "Try Again" (resets state) and "Restart App" (using `Updates.reloadAsync()` from `expo-updates`) CTA actions. Wrapped `SplashScreen.hideAsync()` in `app/_layout.tsx` in a `try/catch` block to handle Android edge-case exceptions. Set up a global unhandled rejection / error listener via `ErrorUtils` to log unexpected promises and fatal crashes. Safeguarded local data mutations `addDocument` and `deleteDocument` in `src/store/documentStore.ts` with `try/catch` handlers. Safeguarded all asynchronous `expo-notifications` API calls inside `src/lib/notifications.ts` via local try/catch blocks. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Feature 26 (Global Search Screen)**: Created `src/app/search.tsx` as a full-screen search experience, supporting search across document names, categories, notes, and expiry dates using local store documents filtered with `useMemo`. Integrated a 300ms text input debounce to save search queries into a recent searches history list (max 5, deduplicated) persisted in AsyncStorage under `"zentra_recent_searches"`. Tapping a recent search populates the input, and clicking "Clear" clears the history from state and AsyncStorage. Highlighted matching query text by bolding the matching substring in document names inline in the results list. Re-routed the Home screen search bar (`src/app/(tabs)/index.tsx`) to act as a static pressable linking directly to `/search`, and removed the local search state and result components to centralize search. Verified that type safety compiles cleanly with `bunx tsc --noEmit` (0 errors).
- **Refactoring (Search Relocation to Home Screen)**: Moved the search system, interactive state, and filtering logic from the Documents tab screen (`src/app/(tabs)/documents.tsx`) to the Home screen (`src/app/(tabs)/index.tsx`). Replaced the static search bar pressable on the Home screen with an interactive `TextInput` field and clear button matching the Zentra design system. Filtered documents matching the search query case-insensitively and displayed them using the existing `RecentDocRow` layout inside a premium card container on the Home screen. Rendered a "No results found" `EmptyState` when queries yielded zero matches. Simplified folder and document views on the Documents tab to focus purely on category filters and tabs without search input overlap. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Bug Fix (Persistent Expiry Alerts Read State)**: Implemented persistent read/unread state tracking for document expiry alerts. Refactored the Zustand store (`src/store/documentStore.ts`) to maintain a `readAlerts: string[]` state slice representing document IDs whose expiry alerts have been marked as read. Persisted `readAlerts` in AsyncStorage, exposed `markAlertAsRead` and `markAllAlertsAsRead` actions, and wired automatic reset logic to mark alerts as unread again if their document's expiry date is updated. Connected `src/app/alerts.tsx` to use this persisted store state, and updated the Home screen header (`src/components/DashboardHeader.tsx`) to display the red bell badge dot only when there are unread critical or expired alerts. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Refinement (Add/Edit Document Screen Polish)**: Polished the Add and Edit Document screens and form fields (`src/components/AddDocumentForm.tsx`, `src/components/DatePickerField.tsx`, `src/components/FilePickerButton.tsx`, `src/app/add-document.tsx`, `src/app/edit-document/[id].tsx`) using `impeccable` design principles. Standardized label hierarchy by coloring required asterisks (`*`) in red (`text-danger`) and styling optional parameters with muted gray text (`text-secondary font-normal text-body-sm`). Set consistent input container heights (`height: 52` for single-line text inputs and `h-[52px]` / `minHeight: 52` for DatePicker triggers) and reset text input vertical padding to `0` to ensure perfect vertical text centering and alignment between text inputs and DatePicker triggers. Resolved a visual bug where the DatePicker input box rendered without borders on native devices by explicitly adding `border border-border` to the class list and setting `borderStyle: "solid"` in the Pressable's inline styles. Resolved a Web-specific styling bug where the DatePicker input trigger text displayed without horizontal padding (flush to the left border) by moving the layout padding and height properties (`px-4 h-[52px]`) from the Pressable's dynamic inline style object into the static `className` list. Realigned layout scroll padding by removing `px-6` from modal layout screens and placing the `24px` horizontal padding inside the scroll content container, which ensures full-width swipe responsiveness and properly aligns scrollbars at screen edges. Enlarged touch targets for category pills and file-type select tabs to `py-3` for easier tapping. Added `px-6` padding to both "Cancel" and "Save Document" buttons for standard design-system conformance and visual breathing room. Added `underlineColorAndroid="transparent"` to TextInputs to prevent default OS-specific decoration lines. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Bug Fix (Favorites Screen Implementation)**: Fully implemented the Favorites screen (`src/app/favorites.tsx`) which was previously a placeholder. Integrated it with `useDocumentStore` to filter and display only favorited documents (`isFavorite: true`). Rendered them in a scrollable list using `DocumentCard` with `"list"` viewMode, with styling matching the list shadow design of the documents tab. Handled the empty favorites state by integrating the `EmptyState` component with a redirect action to the documents list. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Refinement (Profile Screen Polish)**: Polished Zentra's Profile (Settings) screen using `impeccable` design principles. Aligned the screen structure with the Home and Documents screens by converting the fixed white navigation header into a scrollable transparent header, and card-wrapping the top User Identity section so it matches the surrounding cards. Corrected the `bg-softAccent` class typo to the theme token `bg-soft-accent`. Replaced `Ionicons` menu list icons with matching outline `Feather` icons (`heart` for Favorites, `bell` for Expiry Alerts) to ensure icon uniformity. Cleaned up section header visual weight and clutter by removing horizontal line borders and backgrounds, styling them as spacious text labels. Styled the default initials profile avatar with a premium `bg-soft-accent` background and indigo `text-accent` initials text, enclosed in a soft-accent border. Wrapped the name edit modal content in a `KeyboardAvoidingView` to prevent screen overlapping when typing on mobile. Added active focus borders (indigo `border-accent border-2` on focus) to modal inputs and active tapping feedback to notice chips. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Bug Fix (Native Module Availability - ExpoSharing & ExpoIntentLauncher)**: Fixed a runtime crash (`Error: Cannot find native module 'ExpoSharing'`) when clicking the "Share" button or opening files on environments without the native sharing or intent launcher binaries. This was caused by Metro evaluation of dynamic requires still loading the native dependency at runtime and throwing native module errors. Resolved by wrapping the requires in a safe module-existence check using `requireOptionalNativeModule` from `expo-modules-core` before importing. If the module is missing, the code paths gracefully display a descriptive alert to the user instead of throwing an unhandled exception.
- **Bug Fix (DatePicker Modal Horizontal Collapse)**: Resolved a critical layout bug on native platforms where opening the Expiry Date picker modal rendered the calendar card collapsed horizontally into a thin vertical line down the center of the screen. This was caused by NativeWind v5/Tailwind v4's class-based width (`w-full`) and max-width (`max-w-lg`) collapsing under React Native's layout calculation when combined with `self-center` inside a non-stretched modal overlay. Applied explicit inline styles `style={{ width: "100%", maxWidth: 512 }}` directly on the Bottom Sheet Card view, and added explicit `width: "100%"` and `height: "100%"` to the overlay container (`styles.modalOverlay`) to ensure it scales correctly across all screen sizes.
- **Refinement (DatePicker UI & Input)**: Polished the date picker field trigger input and bottom sheet calendar selector UI using the `impeccable` design principles. Cleaned up input trigger redundancy by removing redundant static text and displaying only the formatted selected date or "Select date" placeholder, styled with correct typography fonts, sizes, and colors. Refined calendar bottom sheet styles with `font-display` (Outfit) for headings, month titles, buttons, and dates. Highlighted today's date in a soft accent circle with an indigo text and border (when not selected). Corrected NativeWind camelCase classes like `bg-softAccent` to `bg-soft-accent` to conform to Tailwind v4 theme specifications.
- **Bug Fix (Date Picker Layout / Wrapping)**: Resolved a calendar layout bug where the Days Grid rendered squished into a single straight horizontal row instead of wrapping. This was caused by NativeWind v5/Tailwind v4 parsing of `flex-wrap` failing to map correctly on native devices. Resolved by explicitly applying inline styles `style={{ flexDirection: "row", flexWrap: "wrap" }}` to the calendar days grid container in `src/components/DatePickerField.tsx`. Proactively applied the same robust layout wrapping styles to the category pills wrapper in `src/components/AddDocumentForm.tsx` and the document grid list in `src/app/(tabs)/documents.tsx`. Verified type safety and clean compilation.
- **Feature 25 (Share Document)**: Implemented document sharing on the Document Details screen using `expo-sharing` and `expo-file-system/legacy`. Created a helper in `src/lib/share.ts` with `mimeTypeFor` (mapping file types to MIME types) and `buildDocumentSummary` (formatting document details for text summary). Modified `src/app/document/[id].tsx` to check sharing availability (`Sharing.isAvailableAsync()`) and handle two sharing paths: (1) documents with an attached file (`localUri`) verify file existence via `FileSystem.getInfoAsync` and share using `Sharing.shareAsync` with the correct MIME type and title; (2) metadata-only documents generate a plain text summary, write it to a temporary `zentra_share.txt` file in `cacheDirectory`, share it, and clean up the temp file after. Fixed startup crashes on environments without native module binaries (like Web/Expo Go/unlinked simulators) by refactoring the native dependencies `expo-intent-launcher` and `expo-sharing` in `src/components/FileViewer.tsx` and `src/app/document/[id].tsx` to use dynamic requires at runtime instead of top-level imports. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Feature 24 (Sort Control on Documents Screen)**: Added a sort control to the Documents screen so users can order their document list by name (A → Z), expiry date (soonest first), or date added (newest first). The sort control displays as a horizontal row of 3 text-pill buttons and is hidden when the category folder view is active (i.e., when `showFolders` is true). Sorting uses `useMemo` on the filtered documents list. When the sort is `"expiry"`, a subtle inline `ExpiryBadge` (with `hideSafe={false}`) is conditionally rendered inside the screen's list render for safe documents (unsafe documents already show it, preventing visual duplication). Wrapped grid and list layout items in relative containers and set `pointerEvents="none"` on the badge container to preserve clickability. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Feature 23 (Document File Viewer)**: Implemented file viewing for attached documents on the Document Details screen. Installed `expo-file-system`, `expo-sharing`, and `expo-intent-launcher` (standard Expo SDK packages). Created `src/components/FileViewer.tsx` with two viewing modes: (1) Full-screen image viewer modal with pinch-to-zoom, pan, and double-tap-to-zoom using `react-native-gesture-handler` and `react-native-reanimated`, and (2) External viewer fallback for PDF/doc/other file types using `IntentLauncher` on Android and `Sharing.shareAsync` on iOS. Added a "View File" action row (with `eye` icon in accent color) to the actions section in `src/app/document/[id].tsx`, positioned above Share. The row only renders when `doc.localUri` is non-empty. Before opening, the handler validates file existence using `FileSystem.getInfoAsync()` — if the file is missing, an alert is shown instead of crashing. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Feature 22 (Notification Deep Linking)**: Wired notification tap handling so that tapping an expiry reminder notification navigates the app directly to the relevant Document Details screen. Added an `InitialLayout` component inside `ClerkProvider` in `src/app/_layout.tsx` with two `useEffect` hooks: (1) foreground and background/active notification listeners using `addNotificationReceivedListener` and `addNotificationResponseReceivedListener`, and (2) killed-state cold start handling via `Notifications.getLastNotificationResponseAsync()` that runs only after store hydration (`_hasHydrated`) and Clerk auth load (`isLoaded`). All navigation is guarded by verifying the `documentId` exists in the Zustand `documents[]` store before routing — if the document was deleted, navigation is silently skipped. The `data: { documentId: doc.id }` payload was already present in `lib/notifications.ts`. Verified type safety with `bunx tsc --noEmit` (0 errors).
- **Feature 21 (Profile Screen (Merged with Settings))**: Replaced the placeholder Profile tab screen with a complete, single-destination settings and profile screen. Covered user identity card (Clerk image or initials avatar), live stats boxes (Total, Expiring Soon, and Favorites counts), My Content navigation (Favorites and Expiry Alerts), Notifications preferences (Global Toggle + multi-select advance notice pills + display-only reminder time), Security (App Lock status), Data ("Delete All Documents" with warning Alert confirmation), About section (dynamic app version via `expo-constants` and tagline with shield icon), and a red Sign Out button. Added `clearAllData` store action to support wiping local documents and clearing local notifications. Integrated custom Clerk profile editing features: editing first and last names via a custom modal (`user.update()`), updating user profile photo via camera capture or photo library (`user.setProfileImage()`) using `expo-image-picker` with base64 conversion and safe require checks, and removing custom photos. Verified type safety and clean compile.
- **Bug Fix (Native Module Availability & Startup Crashes)**: Resolved a startup crash (`RuntimeError: Cannot override the host object for expo module 'ExpoImagePicker'`) and a native module loading error (`Cannot find native module 'ExpoDocumentPicker'`). The root cause was that `global.expo.modules` is a JSI HostObject controlled by the C++ native layer, meaning JS assignments directly to it are blocked and throw errors. Removed the mock assignments to `global.expo.modules` in `index.js`. Corrected the native module name for `expo-image-picker` from `"ExpoImagePicker"` to `"ExponentImagePicker"` in both `index.js` and `src/components/FilePickerButton.tsx`. Since native package imports are wrapped in conditional run-time `require()` statements, the packages are never loaded or evaluated when their corresponding availability flags are false, which successfully prevents `Cannot find native module` crashes on platforms without these binaries without requiring any HostObject mock mutations. Verified clean compilation and 0 linter errors.
- **Feature 20 (Edit Document Screen)**: Added the ability to edit an existing document's details. Reused `components/AddDocumentForm.tsx` by introducing an `initialValues?: ZentraDocument` prop. Pre-filled all form fields when provided, and dynamically updated the submit button text to "Update Document". Wired the `FilePickerButton` to display and handle file attachments in the form, pre-populating with existing `localUri`, name, size label, and fileType. Created a new edit document screen route `app/edit-document/[id].tsx` with matching layouts, which manages document state and rescheduled or canceled notifications via `lib/notifications.ts` based on the document's notifications state. Wired the three-dot menu on the Document Details screen (`src/app/document/[id].tsx`) to trigger an `Alert.alert` action sheet with options to edit (✏️ Edit) and delete (🗑 Delete) the document. Removed the bottom delete row from the actions list card and adjusted the remaining layout borders. Verified that everything compiles cleanly and passes type checking.
- **Feature 19 (File Upload - Camera, Gallery, Document Picker)**: Installed `expo-image-picker` and `expo-document-picker` libraries. Created the `FilePickerButton` component (`src/components/FilePickerButton.tsx`) supporting camera capture, photo library selection, and document browsing. To prevent startup crashes when native document/image picker binaries are missing, the imports were refactored to load dynamically at runtime via inline requires. Since Metro's eager module evaluation and require hoisting still executed those requires during Expo Router's startup route validation (before the component or file-level code ran), the mock registration was moved to a newly created root `index.js` entrypoint. This file checks native availability using `requireOptionalNativeModule` from `expo-modules-core` at startup, registers dummy mocks on `globalThis.expo.modules` before booting `expo-router/entry` to satisfy wrapper imports, and caches the original presence flags (`__isImagePickerNativeAvailable` and `__isDocumentPickerNativeAvailable`) on `globalThis` for `FilePickerButton` to determine whether it should fallback to sandbox mode. Configured `package.json`'s main field to load `index.js` first. Declared both `expo-document-picker` and `expo-image-picker` in `app.json`'s `plugins` array to ensure correct native configurations on prebuild. Implemented strict permission checks with setting redirection alert gates on denial. Configured automatic size label calculation from raw bytes and document file-type inference based on MIME-type and URI extensions. Color-coded card rendering states to match Zentra's core styles. Integrated the picker directly above the notes input in `AddDocumentForm` (`src/components/AddDocumentForm.tsx`) with automatic form state updating and document name field auto-filling on selection. Verified 100% type safety and successful compile.
- **Feature 18 (Proper Date Picker Component)**: Created a premium custom `<DatePickerField>` component (`src/components/DatePickerField.tsx`). To resolve the native binary runtime crash (`RNCDatePicker could not be found`) in Expo Go/development client environments, the native picker was replaced with a custom pure-JavaScript slide-up calendar modal on native platforms (iOS and Android). The modal is styled using NativeWind and styled design tokens, featuring cancel/done controls, month-to-month navigation, weekday headers, and a 7-column calendar grid. The grid disables past dates (with today as the minimum selection) and highlights the chosen date in Zentra's indigo accent color (`#4F46E5`). Web continues to use an absolutely positioned transparent native HTML date input overlay. Replaced the plain text input in `<AddDocumentForm>` (`src/components/AddDocumentForm.tsx`) with `<DatePickerField>`, ensuring identical state wiring. Verified 100% type safety and zero ESLint errors.
- **Feature 17 (Empty States, Loading States & Error States)**: Polished all Zentra screens with scenario-specific empty states, loading indicators, and error feedback. Refactored the `EmptyState.tsx` component to take an optional action label and onAction handler, showing an accent-colored button when provided. Integrated `EmptyState` in the Home screen (`app/(tabs)/index.tsx`) for empty vaults, and hid the Quick Access section when there are no items. Implemented conditional empty states in the Documents screen (`app/(tabs)/documents.tsx`) to handle empty vaults, search queries yielding 0 results, and empty category folders. Configured the Document Details screen (`app/document/[id].tsx`) to render `EmptyState` with a back button when a document ID is not found. Added `_hasHydrated` state flag to the Zustand store (`documentStore.ts`) and configured the root layout (`app/_layout.tsx`) to show a centered `ActivityIndicator` loader on cold start until the store rehydration is complete, avoiding empty screen flashes. Verified 100% type safety and successful TypeScript compilation.
- **Feature 16 (NotificationToggle Component + Per-Document Notification Management)**: Created the `NotificationToggle` component (`src/components/NotificationToggle.tsx`) which encapsulates native Switch toggling, checks permission on-device with `hasPermission()`, requests permissions via `requestPermissions()`, and triggers a settings Alert on denial. Integrated the toggle under the expiry date row in Document Details (`src/app/document/[id].tsx`) to schedule/cancel local document notifications via `expo-notifications`, and in `AddDocumentForm` (`src/components/AddDocumentForm.tsx`) to guarantee consistent permission gates. Built a global notifications toggle row inside the Profile screen (`src/app/(tabs)/profile.tsx`) that controls global settings, clears all scheduled alerts when turned OFF, and reschedules alerts for all active documents when turned ON. Verified 100% type safety and successful TypeScript compilation.
- **Feature 15 (Expiry Alerts / Notification Center)**: Implemented Expiry Alerts screen (`src/app/alerts.tsx`) and empty state component (`src/components/EmptyState.tsx`). Grouped upcoming expiries into 4 urgency sections (Expired, This Week, This Month, Next 3 Months) using `expiryUrgency` and `daysUntilExpiry` from `lib/date.ts`, and sorted each group ascending using `sortByExpiry`. Implemented a session-local unread state (`useState<Set<string>>`) so that tapping a row marks it as read and clears its indicator, and added a "Mark all read" header button to clear all indicators. Wired the Home screen bell icon (`src/components/DashboardHeader.tsx`) to display a red badge dot if any documents are Expired or Critical, and navigate to the alerts screen on tap. Verified 100% type safety and successful TypeScript compilation.
- **Feature 14 (Full Profile Screen)**: Replaced Zentra's Profile tab placeholder (`src/app/(tabs)/profile.tsx`) with a real Clerk user profile, document counts, and action menu. Configured the user card with Clerk's profile avatar (fallback to initials circle using first and last name), large display name, and primary email, as well as an "Edit Profile" link triggering an alert guide. Formatted a stats row with 3 boxes showing real-time statistics from Zustand (Total Docs, Expiring Soon via `expiryUrgency`, and Favorites). Built a structured list of menu rows with Feather icons and chevrons grouped by section ("My Content", "Preferences", "Support"). Configured About Zentra details modal and real Clerk Sign Out confirmation flow, which clears the session and local user store state without clearing document data, redirecting back to `/sign-in`. Created placeholder screens (`favorites.tsx` and `alerts.tsx`) to support navigation links. Verified 100% type safety and successful TypeScript compilation.
- Added safe back navigation fallbacks in the add-document and document detail flows to avoid GO_BACK warnings when no history exists.
- Cleaned up lint warnings across auth, documents, and components (array type style, unused imports/vars).
- Synced feature specs (features 9-13), updated document store scheduling logic, and tightened UI/date utilities for Documents, Home, and Document Details screens.
- **Feature 13 (Local Notifications & expo-notifications Setup)**: Created `src/lib/notifications.ts` to manage all local notification scheduling and permissions on the device. Configured the module-level handler with custom `shouldShowBanner` and `shouldShowList` overrides for type safety under strict rules. Exported functions for requesting permissions, checking current status, scheduling, and canceling alerts using the custom prefix `${documentId}-${daysBeforeExpiry}d` convention, and retrieving active scheduled requests. Verified full type safety and TypeScript compilation (`tsc --noEmit`) with 0 errors.
- **Feature 12 (Add Document Screen)**: Implemented Zentra's Add Document screen (`src/app/add-document.tsx`), the `AddDocumentForm` component (`src/components/AddDocumentForm.tsx`), and a temporary notification stub library helper (`src/lib/notifications.ts`). The screen is presented as a native slide-up modal with responsive Safe Area top/bottom padding and keyboard-avoiding container blocks. The form supports validation checks (required name and YYYY-MM-DD calendar formatted date fields), horizontal pill layouts for category/filetype selectors, note areas, and on-device switch toggles for expiry notifications. Successfully integrated save actions to update the Zustand persisted store and go back. Verified 100% type safety and successful TypeScript compilation.
- **Feature 11 (Document Details Screen)**: Implemented Zentra's Document Details screen (`src/app/document/[id].tsx`) and updated the `ExpiryBadge` component (`src/components/ExpiryBadge.tsx`). ExpiryBadge now supports a green "safe" status pill and a `hideSafe` toggle logic. The details screen renders a visual header dynamically padded using `useSafeAreaInsets` to resolve status bar overlapping, document visual block, center-aligned ExpiryBadge, light divider-bordered information lists with dynamic Clerk user name binding, and a card list of actions including interactive heart favorites toggling and trash delete actions. Verified 100% type safety and successful TypeScript compilation.
- **Feature 10 (Documents List Screen)**: Implemented Zentra's Documents tab screen (`src/app/(tabs)/documents.tsx`), the reusable `DocumentCard` component (`src/components/DocumentCard.tsx`), and the warning alert helper `ExpiryBadge` component (`src/components/ExpiryBadge.tsx`). Configured a fully responsive grid and list view layout with inline state-controlled toggle buttons, real-time search filtering, dynamic file-type category tab selectors, and folder categories detailing counts from the persisted Zustand store. Configured a placeholder route details page at `src/app/document/[id].tsx` for navigation. Verified 100% type safety and successful TypeScript compilation.
- **Feature 9 (Home Screen)**: Implemented Zentra's Home tab screen (`src/app/(tabs)/index.tsx`) and the reusable `DashboardHeader` component (`src/components/DashboardHeader.tsx`) matching the premium light-theme design exactly. Configured dynamic Clerk user profile first name loading, local Zustand documents store binding (sorting by `createdAt` descending, showing the top 3 most recent in Quick Access and top 4 in Recent Documents), custom file type icon mappings, date-fns formatting for relative added dates (Today, Yesterday, exact date), a fixed Floating Action Button (FAB) with custom shadow elevation, and an interactive empty state with a "Seed Demo Documents" action to facilitate verification. All checks compiled cleanly under TypeScript.
- **Feature 8 (Date & Expiry Logic)**: Created the date calculation helper utility module `src/lib/date.ts` to centralize all document expiration calculations. Installed `date-fns` and exported 8 helper functions: `daysUntilExpiry`, `isExpired`, `isExpiringSoon`, `expiryLabel`, `formatDate`, `sortByExpiry`, `filterUpcoming`, and `expiryUrgency`. Validated all helpers using a scratch test script, and verified that the codebase passes TypeScript compilation checks (`tsc --noEmit`).
- **Feature 7 (Zustand Store + AsyncStorage)**: Created the global Zustand store (`useDocumentStore` in `src/store/documentStore.ts`) persisting to AsyncStorage to act as the on-device single source of truth for Zentra's local data model. Implemented state actions (`setUser`, `addDocument`, `updateDocument`, `deleteDocument`, `toggleFavorite`, `toggleNotification`, `updateNotificationSettings`) and automatic derived calculation of `upcomingExpirations` (documents expiring within 90 days sorted by soonest). Verified state transitions and strict TypeScript compilation.
- **Feature 6 (Bottom Tab Navigation)**: Implemented bottom tab navigation with Home, Documents, Collections, and Profile tabs. Configured branding colors (active indigo `#4F46E5`, inactive gray `#737373`), custom labels (Inter, 11px, medium weight), white background, and a top border `#E5E7EB`. Created placeholder screens for each tab and set up automatic redirection for authenticated users from the root to the tabs group. Installed `@expo/vector-icons` for modern Feather outlined icons.
- **Feature 5 (TypeScript Types)**: Defined shared TypeScript interfaces and types for documents, notifications, and user models.
- **Feature 4 (Real Clerk Auth)**: Successfully integrated Clerk SDK for real authentication, replacing all mock flows.
- **Feature 3 (Auth UI)**: Built the Sign Up, Sign In, and Verification UI matching the Zentra/Memora light-theme design.
- Set up NativeWind v5 and Tailwind CSS v4 in Expo App.
- Created `postcss.config.mjs` and `src/global.css`.
- Configured `metro.config.js` with `withNativewind`.
- Added `lightningcss` override to `package.json`.
- Imported `global.css` into `src/app/_layout.tsx`.
- Added TypeScript types in `nativewind-env.d.ts`.
- Added design tokens/utilities in `src/theme` and wired them into `global.css`.
- Added Inter + Outfit variable fonts and Expo font loading in the root layout.
- Fixed `postcss.config.mjs` to use the Tailwind PostCSS plugin.
- Reviewed feature2.md implementation; verified correct fonts, colors, and layout utilities.
- Added missing `spacing` and `z-index` tokens to `src/theme/tokens.css`.
- Moved auth screens to `(auth)` Expo Router group.
- Implemented `Sign Up` screen matching the design.
- Implemented `Sign In` screen based on `Sign Up` UI.
- Implemented `VerificationModal` with 6-digit number pad and auto-navigation.
- Added `constants/images.ts` for centralized image management.
- Redesigned Auth screens based on Memora light-theme HTML UI design (`code-design.md`).
- Loaded the serif `PlayfairDisplay-Italic.ttf` font and registered it in the theme config for branding.
- Downloaded and integrated the official Google "G" logo PNG icon for the social auth option.
- Styled form inputs, password fields, divider, and spacing details to exactly match the reference design and reference image.
- Applied explicit `width: "100%"` inline style fixes on layout containers, inputs, buttons, and rows to prevent horizontal collapsing in React Native Web.
- Configured root stack layout to hide native headers globally (`screenOptions={{ headerShown: false }}`), removing the "(auth)" header from the sign-up and sign-in screens.
- Updated root index route to redirect automatically to `/sign-up` using `<Redirect />`, removing the temporary navigation links.
- Polished the Sign Up and Sign In screens with interactive focus borders for inputs, active pressed states for buttons and links, and brand-tinted neutrals.
- Polished the Verification Modal using a premium 6-digit OTP box pattern (separate boxes with focus highlight states over an invisible text input overlay).
- Implemented client-side email and password form validation on sign-up and sign-in screens with inline error messages.
- Added password visibility toggling (Show/Hide) with adaptive styles to prevent typography size/tracking distortion when text is visible.
- Resolved the horizontal layout collapse issue on React Native Web for the `VerificationModal` by applying explicit inline style dimensions (`width: "100%", height: "100%"` on wrapper containers and `width: "100%"` on the OTP boxes container).
- Removed the `VerificationModal` trigger from the `Sign In` (login) screen, routing the user directly to the home/dashboard screen instead.
- Fixed `VerificationModal` OTP input focus and interactivity issues on iOS, Android, and Web by setting style opacity to `0.01` (preventing OS layout/security engines from blocking programmatic focus on hidden elements) and configuring transparent colors.
- Polished the `VerificationModal` UI/UX into a flagship-grade experience by introducing a custom blinking caret inside the active OTP cell, a 30s resend timer with successful resend feedback, and simulated secure verification (loading spinner) and success (green checkmark) states.
- Fixed native keyboard focus and interactivity blocks in Expo Go by handling the Modal's `onShow` transition callback with a deferred `.focus()` call, setting `pointerEvents="none"` on the hidden `TextInput` to ensure touch events reliably bubble to the parent trigger, and setting `fontSize: 1` to prevent Android text rendering/selection glitches.
- Integrated Clerk SDK provider in the root layout with secure token caching and added the Expo publishable key env placeholder.
- Implemented real Clerk authentication logic in `Sign In` and `Sign Up` screens using `useSignIn` and `useSignUp` hooks.
- Integrated Google Auth using Clerk's `useSSO` hook for seamless social authentication.
- Updated `VerificationModal` to handle real email verification with Clerk, including OTP attempt and session activation.
- Configured auth route protection in `(auth)/_layout.tsx` to redirect authenticated users to the root.
- Updated root index route (`src/app/index.tsx`) to act as a secure gateway, redirecting unauthenticated users to sign-in and showing a dashboard placeholder for authenticated users.
- Resolved Clerk Core 3 SDK integration issues across `sign-in.tsx`, `sign-up.tsx`, and `VerificationModal.tsx` by updating hook destructuring, creating custom image asset TypeScript declarations, and using the correct `.verifications` and `.finalize` API flows.
- Successfully verified that all TypeScript compilation issues are resolved in the project (`tsc --noEmit` passes with 0 errors).
- Resolved multiple configuration, dependency locking, stylesheet styling, component code quality, and strict TypeScript compilation errors across layout, authentication screens, and modals.
- Wired up the "Forgot?" link in the Sign In screen (`sign-in.tsx`) to redirect users to `/forgot-password`.
- Refactored `forgot-password.tsx` to use the modern Clerk v3 SDK `resetPasswordEmailCode` helper API instead of the legacy `attemptFirstFactor` method.
- Re-architected the Forgot Password UI/UX into a 3-step sequence: Send Code -> Verify Code -> Set New Password.
- Integrated a premium countdown-based resend OTP timer on the code verification step.

## In Progress

- None.

## Next Up

- Implementation of the local Document CRUD functionality.
- Setup of local notification scheduling logic.
- Integration of `expo-file-system` for local document attachments.

## Open Questions

- None currently.

## Architecture Decisions

## Session Notes

- **Feature 5 (TypeScript Types)**: Created modular type definitions in `src/types/` (`document.ts`, `notification.ts`, `user.ts`, `index.ts`) matching requirements exactly, ensuring stable type safety for all upcoming features.
- **Feature 4 (Real Clerk Auth)**: Successfully migrated from mock authentication to real Clerk SDK integration. Implemented full Sign Up, Sign In, and Forgot Password flows. Integrated Google SSO using `useSSO`. Wired up real email verification in the `VerificationModal`. Fixed all Clerk Core 3 SDK compatibility issues and ensured 100% TypeScript type safety across the auth surface.
- **Feature 3 (Auth UI)**: Built the Sign Up and Sign In screens entirely locally based on the Zentra design image. Created a custom `VerificationModal` with a 6-digit number pad for email validation. All UI styling adheres strictly to the existing NativeWind setup, with inline style overrides specifically for custom font families (`Outfit` and `Inter`) to ensure accurate rendering.
- **Bug Fix & Code Quality Polish**:
  - Fixed documentation typos, grammatical errors, and outdated scanner references across content and markdown files.
  - Implemented mount guard refs in Profile screen timeouts (`profile.tsx`) to prevent state updates after unmount.
  - Replaced sequential loops for local notification scheduling with parallel `Promise.all` execution.
  - Refactored `DatePickerField` to use strongly-typed `React.CSSProperties` on web, eliminating typescript `as any` casts.
  - Aligned alerts screen pill counts to match list section logic.
  - Enforced global notification settings checks during individual document toggle changes.
  - Added unique Date.now() timestamp suffix to temp share files to prevent concurrent share clobbering.
  - Added category fallback list logic to ensure selected category is visible even if folders are empty.
  - Permitted unchanged past expiry dates during document editing validation.
  - Standardized image picker options using recommended non-deprecated string lists (`["images"]`).
  - Corrected file viewer trigger documentation comments.

